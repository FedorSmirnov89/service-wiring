//! Placeholder for the crate which has all the definitions of the types we use in the system

pub struct EventOne {
    pub count: i32,
}
pub(crate) struct EventTwo {
    pub(crate) count: u32,
}
pub(crate) struct EventThree {
    pub(crate) count: f64,
}

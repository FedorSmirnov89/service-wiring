//! this is a "mock" for the crate which would define all of our events

pub struct EventOne {
    pub count: i32,
}
pub struct EventTwo {
    pub count: u32,
}
pub struct EventThree {
    pub count: f64,
}
